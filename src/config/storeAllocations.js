// Store-to-agent allocations were intentionally cleared on 4 August 2026.
// Original onboarding ownership remains in the live acquisition sheet.
export const STORE_ALLOCATIONS = Object.freeze({
  Peace: Object.freeze([]),
  Queen: Object.freeze([]),
  Ifeoma: Object.freeze([]),
});

export function getAssignedStores() {
  return [];
}

export function getAssignedStoreRecords() {
  return [];
}

export function getAllAssignedStoreRecords() {
  return [];
}
